// import {Metadata} from './metadata';

export interface Favorite {
  // _id: string; // TODO I don't remember if we're given a id or _id
  // ownerId: string;
  name: string;
  folderPath: string;
  // index: number;
  // links: string[];
  // metadata: Metadata;
}
